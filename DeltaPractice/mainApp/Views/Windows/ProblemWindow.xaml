<Window x:Class="mainApp.Views.Windows.ProblemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:mainApp.ViewModels.Windows"
        mc:Ignorable="d"
        Title="ProblemWindow" Height="450" Width="800">

    <Window.DataContext>
        <local:ProblemViewModel />
    </Window.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto">

        <StackPanel Margin="20">

            <ItemsControl ItemsSource="{Binding ContentItems}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.Resources>
                    <DataTemplate DataType="{x:Type local:TextViewModel}">
                        <StackPanel Margin="0,10,0,10">
                            <TextBlock Text="{Binding Text}" FontWeight="Normal"/>
                        </StackPanel>
                    </DataTemplate>

                </ItemsControl.Resources>
            </ItemsControl>

            <Button Content="Submit" Margin="0,20,0,0" Width="150" HorizontalAlignment="Center"/>

        </StackPanel>
    </ScrollViewer>
</Window>
